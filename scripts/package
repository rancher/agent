#!/bin/bash
set -e

source $(dirname $0)/version

cd $(dirname $0)/..

mkdir -p build
cd build

mkdir -p bin
cp ../bin/agent bin/
cp ../apply.sh .

mkdir -p ../dist/artifacts
tar cvJf ../dist/artifacts/go-agent.tar.xz .

echo Created ../dist/artifacts/go-agent.tar.xz
