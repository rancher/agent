package apiproxy

func StartUp() {
	/*
		url := utilities.ConfigURL()

		if !strings.Contains(url, "localhost") {
			return
		}
			parsed, _ := urls.Parse(url)

			fromHost := utils.ApiProxyListenHost()
			fromPort := utils.ApiProxyListenPort()
			toHostIp :=
	*/
}
